# sops-nix secrets template
# Copy this to secrets.yaml in each host directory
# Generate age key: age-keygen -o keys.txt

# Example secrets structure:
# pppoe-password: ENC[AES256_GCM,base64string...]
# luks-key: ENC[AES256_GCM,base64string...]
