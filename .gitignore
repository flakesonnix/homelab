# Nix
result
result-*
*.drv
*.nar
.direnv

# Editor
*.swp
*.swo
*~

# Secrets (sops-nix / age)
# Encrypted per-host secrets ARE committed: hosts/*/secrets.yaml, .sops.yaml.
# Only private key material is ignored.
.sops/
*.age

# Build
.deploy

# Local nix store (nix-serve-ng)
nix/

.ai/
AGENTS.md
CLAUDE.md

# Node/JS
node_modules/

# Obsidian local UI state (vault content itself is committed)
wiki/.obsidian/workspace.json
