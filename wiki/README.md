# wiki/ — Obsidian vault

Obsidian vault for this repo. In Obsidian: **Open folder as vault → `wiki/`**.

- Entry point: [[Home|Home]] (`wiki/Home.md`)
- Graph colors + templates in `wiki/.obsidian/` are committed so everyone gets the same setup.
- Optionally install the community plugin **Dataview** for the queries in `Home.md`, `Hosts-MOC.md`, `99-Meta/Dataview-Queries.md`.
- Source of truth stays `docs/` + code; this vault links there instead of duplicating everything.
- Contains no secrets — only references to sops/age paths. Safe to push to GitHub.
